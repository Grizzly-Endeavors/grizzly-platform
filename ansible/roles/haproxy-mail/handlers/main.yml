---
# haproxy-mail service handlers

- name: Reload haproxy
  ansible.builtin.systemd:
    name: haproxy
    state: reloaded

- name: Restart haproxy
  ansible.builtin.systemd:
    name: haproxy
    state: restarted
