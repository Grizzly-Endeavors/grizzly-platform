; Gate-owned mypy config (passed via --config-file, authoritative over the
; repo's mypy/pyproject settings). Strict by default; loosen deliberately.
[mypy]
python_version = 3.11
strict = True
warn_unused_configs = True
warn_redundant_casts = True
warn_unused_ignores = True
ignore_missing_imports = True
