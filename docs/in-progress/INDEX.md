# In-Progress — thread index

One line per active thread. Delete the line when the thread closes (see [`CLAUDE.md`](CLAUDE.md)).

- [garage-ex50-cutover.md](garage-ex50-cutover.md) — relocate platform to the garage behind the EX50 router; repo work merged, only the physical swap remains (Bear runs it offline).
