apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: career-scanner
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/grizzly-endeavors/career-scanner
  ref:
    branch: main
  provider: github
  secretRef:
    name: github-app-credentials
