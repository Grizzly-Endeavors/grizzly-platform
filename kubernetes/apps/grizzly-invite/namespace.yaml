apiVersion: v1
kind: Namespace
metadata:
  name: grizzly-invite
  labels:
    app.kubernetes.io/name: grizzly-invite
    app.kubernetes.io/managed-by: flux
    # First-party, gate-signed image: Kyverno verifies the cosign signature at
    # admission in gated namespaces (Audit until the enforce cutover).
    grizzly.io/gated: "true"
